#include <stdio.h>
/*
 在说预编译之前，先说一些有关编译程序的问题。
    在你编写完一份代码以后，需要开始执行的时候。此时需要将源代码转化成可以在硬件上运行的程序。这个时候需要进行编译与链接。所谓编译：就是将源代码通过编译器转换成汇编代码，再通过汇编器将汇编代码转化成机器码。而预编译所处理的事情就是在编译阶段之前 所做的事情。
 常用的预编译指令
 1、无参数预编译 #define
 2、含参数的预编译 #define X(x) (x) * 2
 3、条件编译
 */
int main(int argc, const char * argv[]) {
    return 0;
}
